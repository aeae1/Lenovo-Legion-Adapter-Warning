# Frozen internal-release documents

These two files preserve the exact guide and Secure Boot note embedded in the already published `v4-internal-1` ZIP. `scripts/package-internal.py --check` compares the archive against these snapshots while current web documentation can record newer results. The ZIP and its checksum have not changed.

For current status, use [the project status page](../../STATUS.md). Historical pending-test statements in this directory describe the publication checkpoint, not today's result.
